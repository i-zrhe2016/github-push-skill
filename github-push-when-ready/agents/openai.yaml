interface:
  display_name: "Push To GitHub"
  short_description: "Guard every feature commit and GitHub push"
  default_prompt: "Use $github-push-when-ready before committing or pushing. Split independent work into feature-scoped commits, validate each unit, and publish only when the repository is safe."
